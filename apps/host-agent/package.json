{
  "name": "@ccmt/host-agent",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "echo 'lint: host-agent (not configured yet)'",
    "test": "echo 'test: host-agent (not configured yet)'"
  },
  "dependencies": {
    "@ccmt/protocol": "workspace:*",
    "node-pty": "^1.1.0-beta40",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/ws": "^8.5.14",
    "tsx": "^4.19.3",
    "typescript": "^5.8.2"
  }
}
